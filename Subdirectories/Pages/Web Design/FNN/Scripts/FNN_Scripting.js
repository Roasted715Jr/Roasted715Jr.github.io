// JavaScript source code
var main = function () {
    var topArticle = Math.floor(Math.random() * 20); /*Make a random whole number by making a random float
	< 1, multiply it by 20, then round it down to a whole number*/
	
	/*$(".logo").click(function() {
		$("body").animate({
			right: '300px'
		}, 200);
	});*/
}

$(document).ready(main);